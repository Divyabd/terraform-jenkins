pipeline{
    agent any
    stages{
        stage("git checkout"){
            steps{
                echo ""
            }
        }
          stage("terraforrm initt"){
            steps{
                sh 'terraform init'
            }
        }
    }
}
